var i = 10;


// i += 20; Kısa bir kullanım ile i = i + 20 yapmak yerine direkt i += 20; dedik.
// i -= 10; Kısa bir kullanım ile i = i - 10 yapmak yerine direkt i -= 10; dedik.
// i *= 100; // Kısa bir kullanım ile i = i * 100 yapmak yerine direkt i *= 100; dedik.
// i /= 1; // Kısa bir kullanım ile i = i / 1 yapmak yerine direkt i /= 1; dedik.
// i %= 5; // Kısa bir kullanım ile i = i % 5 yapmak yerine direkt i %= 5; dedik.

console.log(i);