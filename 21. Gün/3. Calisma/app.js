/* Math Nesnesi */ 
var input = prompt('Bir değer giriniz');
// var input2 = prompt('Üssü alınacak değeri giriniz');

/*function getCube($input) {
	console.log(Math.cbrt($input));
}

getCube(input);*/

/* Yukarıdaki örnekte Math nesnesinin cbrt metodu ile bir sayının küp kökünü aldık */


/*function getCeil($input) {
	console.log(Math.ceil($input));
}
getCeil(input);*/

/* 
Yukarıdaki örnekte Math nesnesinin ceil metodunu kullanarak girilen değerin tam sayı olmasını sağladık.
*/

/*function getPow() {
	console.log(Math.pow(input, input2));
}
getPow(input, input2);*/

/* 
Yukarıdaki örnekte ise Math nesnesinin pow metodunu kullanarak üs alınmasını sağladık.
*/

// function getFloor(input) {
// 	console.log(Math.floor(input));
// }

// getFloor(input);

/*
Yukarıdaki örnekte ise Math nesnesinin floor metodunu kullanarak aşağı doğru yuvarlanmasını sağladık.
*/ 

// function getRound(input) {
// 	console.log(Math.round(input));
// }

// getRound(input);

/* 
Yukarıdaki örnekte ise Math nesnesinin round metodunu kullanarak girilen değerin yukarı doğru yuvarlanmasını sağladık.
*/

function getSquare(input) {
	console.log(Math.sqrt(input))
}

getSquare(input);
/* 
Yukarıdaki örnekte ise Math nesnesinin sqrt metodunu kullanarak girilen değerin karekökünün alınmasını sağladık.
*/